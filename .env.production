# Frontend production environment (used by CRA at build time)
# Set REACT_APP_API_URL to your deployed backend API endpoint
REACT_APP_API_URL=https://ecommerce-backend-k1lz.onrender.com/api

# Optional timeouts
REACT_APP_API_TIMEOUT=30000

# Cloudinary (if client-side uploads or widget used)
REACT_APP_CLOUDINARY_CLOUD_NAME=dr62ilpwv

# Stripe publishable key (client-side)
REACT_APP_STRIPE_PUBLISHABLE_KEY=replace_with_stripe_publishable_key

# Notes:
# - Set these values in Render static site environment variables before deployment to ensure the build
#   embeds the correct API URL.# Production environment variables for Render deployment
REACT_APP_API_URL=https://ecommerce-backend-k1lz.onrender.com/api
NODE_VERSION=18
